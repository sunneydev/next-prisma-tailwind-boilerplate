import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="bg-black h-screen flex items-center justify-center text-white text-2xl">
      <h1>Next.js + Prisma + TailwindCSS boilerplate</h1>
    </div>
  );
};

export default Home;
